# ── Test framework ────────────────────────────────────────────────────────────
pytest>=7.4

# ── Serial port access (UART + USB CDC) ───────────────────────────────────────
pyserial>=3.5

# ── HTTP client ───────────────────────────────────────────────────────────────
requests>=2.28

# ── YAML config file support (optional – falls back to defaults if missing) ───
PyYAML>=6.0

# ── Nice coloured terminal output (optional but recommended) ──────────────────
# colorama is needed on Windows for ANSI colour codes in the terminal
colorama>=0.4; sys_platform == "win32"
